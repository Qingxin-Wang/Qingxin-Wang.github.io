{% assign project_href = project.redirect | default: project.url | relative_url %}
<li class="project-entry">
  <div class="project-entry__row row align-items-center">
    {% if project.img %}
      <div class="project-entry__media col-12 col-md-4 mb-3 mb-md-0">
        <a href="{{ project_href }}">
          {% include figure.liquid
            loading="eager"
            path=project.img
            sizes="320px"
            alt=project.title
            class="project-thumb z-depth-1 rounded"
          %}
        </a>
      </div>
    {% endif %}
    <div class="project-entry__body {% if project.img %}col-12 col-md-8{% else %}col-12{% endif %}">
      <div class="project-title">
        <a href="{{ project_href }}">{{ project.title }}</a>
      </div>
      {% if project.description %}
        <p class="project-desc">{{ project.description }}</p>
      {% endif %}
      <div class="project-links">
        {% if project.links %}
          {% for key in project.links %}
            {% assign link_url = project.links[key[0]] %}
            <a class="btn btn-sm z-depth-0" role="button" href="{{ link_url }}">{{ key[0] | replace: '_', ' ' | capitalize }}</a>
          {% endfor %}
        {% endif %}
        {% if project.github %}
          <a class="btn btn-sm z-depth-0" role="button" href="{{ project.github }}">GitHub</a>
        {% endif %}
      </div>
    </div>
  </div>
</li>
